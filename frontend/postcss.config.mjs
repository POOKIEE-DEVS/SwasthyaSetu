// Tailwind CSS v4 is configured in CSS (see app/globals.css), so this is the
// whole build-tool integration -- there is no tailwind.config.js.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
