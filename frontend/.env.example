# Local development only -- copy to .env.local.
#
# When `npm run dev` serves the frontend on :3000, API calls must go to the
# backend on :8000. In production the backend serves the frontend itself
# (same origin), so leave this UNSET for production builds.
NEXT_PUBLIC_API_URL=http://localhost:8000
