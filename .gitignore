# Environment files — real secrets live here; only .env.example is committed
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / Next.js
node_modules/
frontend/.next/
frontend/out/
frontend/next-env.d.ts

# Local model weights — MedGemma, whisper.cpp, and the Nepali TTS voice are
# hundreds of megabytes and are fetched, not committed. Anchored to the repo
# root so it cannot swallow backend/app/models/, which is source.
/models/
*.gguf
*.onnx
*.bin

# Editors & OS
.DS_Store
Thumbs.db
.idea/
.vscode/
